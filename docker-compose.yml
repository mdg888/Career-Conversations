services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persists the SQLite database and all uploaded documents between restarts
      - ./backend/data:/app/backend/data
    restart: unless-stopped
